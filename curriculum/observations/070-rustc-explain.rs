// Probe stub for lesson 070-rustc-explain.
//
// The lesson's move is `rustc --explain E0425`. That command takes no
// source file: it is a documentation lookup, not a compilation. So
// there is no `.rs` to compile this cycle; this file exists only as a
// placeholder paper trail at the canonical observations path. The full
// captured transcripts (`rustc --version`, `rustc --explain E0425`,
// `rustc --explain E0601`, and the corpus-page comparison) live in
// `experimental/eduratchet2/runs/rust-moves/evidence/070-rustc-explain.md`.

fn main() {}

fn main() {
    // This creates the first binding named `name`.
    let name = "Eli";
    println!("First: {name}");

    // Repeating `let` creates a new binding with the same name.
    let name = "Rust";
    println!("Second: {name}");
}

fn main() {
    // This binding uses an integer literal instead of a string literal.
    let count = 3;

    // Rust infers the type; no type annotation is written here.
    println!("Count: {count}");
}

fn main() {
    // These bindings hold integer literals.
    let apples = 5;
    let oranges = 7;

    // This condition is false, so the else block runs.
    if apples > oranges {
        println!("More apples");
    } else {
        println!("Not more apples");
    }
}

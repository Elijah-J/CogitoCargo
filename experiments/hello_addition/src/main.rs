fn main() {
    // These bindings hold integer literals.
    let apples = 3;
    let oranges = 4;

    // The + operator evaluates to a new integer value.
    let total = apples + oranges;

    println!("Total: {total}");
}

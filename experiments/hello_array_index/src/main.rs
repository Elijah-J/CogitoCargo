fn main() {
    let numbers = [3, 4, 5];

    // Array indexes start at 0, so index 0 reads the first element.
    let first = numbers[0];
    // Index 1 reads the second element.
    let second = numbers[1];

    println!("first: {first}");
    println!("second: {second}");
}
